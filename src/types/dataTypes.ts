export interface DataShape {
  client: string;
  country: string;
  email: string;
  project: string;
  progress: string;
  status: "Completed" | "In Progress" | "";
  date: string;
  image: string;
}
